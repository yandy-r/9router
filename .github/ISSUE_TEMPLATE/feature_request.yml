name: Feature request
description: "Propose a new feature for 9router"
title: "[Feature]: "
labels: ["type:feature", "status:needs-triage"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for suggesting a feature for **9router**! Please describe what
        you'd like and why it matters.

  - type: dropdown
    id: category
    attributes:
      label: Feature Category
      description: "What area does this feature relate to?"
      options:
        - Translator (format translation)
        - Providers / Executors
        - Dashboard
        - CLI launcher
        - Database / persistence
        - OAuth / credentials
        - Docker / deployment
        - CI / build
        - Documentation
        - Other
    validations:
      required: true

  - type: textarea
    id: problem
    attributes:
      label: Problem / Use Case
      description: "What problem does this solve, or what use case does it enable?"
    validations:
      required: true

  - type: textarea
    id: solution
    attributes:
      label: Proposed Solution
      description: "Describe how you'd like this to work."
    validations:
      required: true

  - type: textarea
    id: alternatives
    attributes:
      label: Alternatives Considered
      description: "Any alternative solutions or workarounds you've considered."
    validations:
      required: false

  - type: checkboxes
    id: duplicate-check
    attributes:
      label: Duplicate Check
      description: "Please search existing issues before filing a new one."
      options:
        - label: "I have searched existing issues and this feature has not been requested."
          required: false
